<html>
    <head>
        <title>View Assigned Duties</title>
        <link rel= "stylesheet" type= "text/css" href= "{{ url_for('static',filename='css/style.css') }}">
    </head>
    <body>
        <a href="/">Return Home</a>
        <div class="flexbox center-text">
            <div class="margin-5 border-gray overflow">
                <h2 class="section-header">Assigned Duty List</h2>
                <table class="border-collapse margin-5 width-auto">
                    <tr>
                        <th>Employee ID</th>
                        <th>Duty ID</th>
                        <th>Actions</th>
                    </tr>
                    {% for assignment in assignments %}
                    <tr>
                        <td>{{ assignment.employeeId }}</td>
                        <td>{{ assignment.dutyId }}</td>
                        <td>
                        <form class="margin-0" action="/store/duty/delete-assignment/{{ assignment.dutyId }}/{{ assignment.employeeId }}" method="post">
                            <button name="delete" value="delete">Delete</button>
                        </form>
                        </td>
                    </tr>
                    {% endfor %}
                </table>
            </div>
        </div>
    </body>
</html>